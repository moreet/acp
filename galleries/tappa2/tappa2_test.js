'use strict';

describe('myApp.tappa2 module', function() {

  beforeEach(module('myApp.tappa2'));

  describe('tappa2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var tappa1Ctrl = $controller('tappa2Ctrl');
      expect(tappa1Ctrl).toBeDefined();
    }));

  });
});
