'use strict';

describe('myApp.premi module', function() {

  beforeEach(module('myApp.premi'));

  describe('premi controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var premiCtrl = $controller('premiCtrl');
      expect(premiCtrl).toBeDefined();
    }));

  });
});
