'use strict';

describe('myApp.classificaTrophy module', function() {

  beforeEach(module('myApp.classificaTrophy'));

  describe('classificaTrophy controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var classificaCtrl = $controller('classificaCtrl');
      expect(classificaCtrl).toBeDefined();
    }));

  });
});
