'use strict';

describe('myApp.trophy module', function() {

  beforeEach(module('myApp.trophy'));

  describe('trophy controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var trophyCtrl = $controller('trophyCtrl');
      expect(trophyCtrl).toBeDefined();
    }));

  });
});
