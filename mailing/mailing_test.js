'use strict';

describe('myApp.mailing module', function() {

  beforeEach(module('myApp.mailing'));

  describe('mailing controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mailingCtrl = $controller('mailingCtrl');
      expect(mailingCtrl).toBeDefined();
    }));

  });
});
