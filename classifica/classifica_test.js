'use strict';

describe('myApp.classifica module', function() {

  beforeEach(module('myApp.classifica'));

  describe('classifica controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var classificaCtrl = $controller('classificaCtrl');
      expect(classificaCtrl).toBeDefined();
    }));

  });
});
