'use strict';

describe('myApp.contatti module', function() {

  beforeEach(module('myApp.contatti'));

  describe('contatti controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var contattiCtrl = $controller('contattiCtrl');
      expect(contattiCtrl).toBeDefined();
    }));

  });
});
