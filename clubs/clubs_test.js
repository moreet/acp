'use strict';

describe('myApp.clubs module', function() {

  beforeEach(module('myApp.clubs'));

  describe('clubs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clubsCtrl = $controller('clubsCtrl');
      expect(clubsCtrl).toBeDefined();
    }));

  });
});
