'use strict';

describe('myApp.convenzioni module', function() {

  beforeEach(module('myApp.convenzioni'));

  describe('convenzioni controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var convenzioniCtrl = $controller('convenzioniCtrl');
      expect(convenzioniCtrl).toBeDefined();
    }));

  });
});
