'use strict';

describe('myApp.storia module', function() {

  beforeEach(module('myApp.storia'));

  describe('storia controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var storiaCtrl = $controller('storiaCtrl');
      expect(storiaCtrl).toBeDefined();
    }));

  });
});
