'use strict';

describe('myApp.regolamento module', function() {

  beforeEach(module('myApp.regolamento'));

  describe('regolamento controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var regolamentoCtrl = $controller('regolamentoCtrl');
      expect(regolamentoCtrl).toBeDefined();
    }));

  });
});
