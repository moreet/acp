'use strict';

describe('myApp.statuto module', function() {

  beforeEach(module('myApp.statuto'));

  describe('statuto controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var statutoCtrl = $controller('statutoCtrl');
      expect(statutoCtrl).toBeDefined();
    }));

  });
});
